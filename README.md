# Word Similarity Package

This Python package provides tools to compute cosine similarity for word embeddings using pre-trained models and visualize the results using boxplots.

## Installation

```bash
pip install .
